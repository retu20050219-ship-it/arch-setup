# #######################################################################################
# AUTOGENERATED HYPRLAND CONFIG.
# EDIT THIS CONFIG ACCORDING TO THE WIKI INSTRUCTIONS.
# #######################################################################################

source = ~/.config/hypr/colors.conf

xwayland{
	force_zero_scaling=true
}
# This is an example Hyprland config file.
# Refer to the wiki for more information.
# https://wiki.hypr.land/Configuring/

# Please note not all available settings / options are set here.
# For a full list, see the wiki

# You can split this configuration into multiple files
# Create your files separately and then link them to this file like this:
# source = ~/.config/hypr/myColors.conf


################
### MONITORS ###
################

# See https://wiki.hypr.land/Configuring/Monitors/
monitor=eDP-1,1920x1080@60,0x0,1.2



###################
### MY PROGRAMS ###
###################

# See https://wiki.hypr.land/Configuring/Keywords/

# Set programs that you use
$terminal = kitty
$fileManager = thunar
$menu = rofi -show drun -theme ~/.config/rofi/launchers/type-7/style-2.rasi 


#################
### AUTOSTART ###
#################

# Autostart necessary processes (like notifications daemons, status bars, etc.)
# Or execute your favorite apps at launch like this:

# exec-once = $terminal
# exec-once = nm-applet &
# exec-once = waybar & hyprpaper & firefox
exec-once = awww-daemon
exec-once = mako & waybar & copyq
exec=once = blueman-applet & nm-applet
exec-once = hyperidle
exec-once = /usr/lib/hyprpolkitagent/hyprpolkitagent
exec-once = sleep 1 && waypaper --restore
exec-once = copyq
exec-once = fcitx5
# permission = /usr/(bin|local/bin)/hyprpm, plugin, allow

##############################
### ENVIRRONMENT VARIABLES ###
##############################

# See https://wiki.hypr.land/Configuring/Environment-variables/

env = XCURSOR_THEME,BreezeX-RosePineDawn-Linux
env = XCURSOR_SIZE,28
#env = HYPRCURSOR_SIZE,24

# Qt程序（微信）使用fcitx
env = Qt_IM_MODULE,fcitx
# X11/XWayland程序（包括 QQ 和X模式下的微信）用fcitx
env = XMODIFIERS,@im=fcitx
env = GTK_IM_MODULE,fcitx
#####################
### LOOK AND FEEL ###
#####################

# Refer to https://wiki.hypr.land/Configuring/Variables/

# https://wiki.hypr.land/Configuring/Variables/#general
general {
    gaps_in = 0 # windows-gaps
    gaps_out = 0 # windows-and-screen-gaps

    border_size = 3

    # https://wiki.hypr.land/Configuring/Variables/#variable-types for info about colors
    col.active_border = $primary rgba(ffffffff) 45deg
    col.inactive_border = rgba(595959aa)

    # Set to true enable resizing windows by clicking and dragging on borders and gaps
    resize_on_border = false

    # Please see https://wiki.hypr.land/Configuring/Tearing/ before you turn this on
    allow_tearing = false

    layout = scrolling
}

# https://wiki.hypr.land/Configuring/Variables/#decoration
decoration {
    rounding = 8
    rounding_power = 2

    # Change transparency of focused and unfocused windows
    active_opacity = 0.8
    inactive_opacity = 0.8

    shadow {
        enabled = true
        range = 5
        render_power = 3
        color = rgba(1a1a1aee)
    }

    # https://wiki.hypr.land/Configuring/Variables/#blur
    blur {
        enabled = true
        size = 5
        passes = 2

        vibrancy = 0.1696
    }
}

# https://wiki.hypr.land/Configuring/Variables/#animations
animations {
    enabled = yes
    bezier = overshot, 0.13, 0.99, 0.29, 1.1


    bezier = overshot, 0.13, 0.99, 0.29, 1.1
    animation = windows, 1, 4, overshot, slide
    animation = windowsOut, 0, 5, default, popin 80%
    animation = border, 1, 5, default
    animation = fade, 1, 8, default
    animation = workspaces, 1, 9, default, slide
}

# See https://wiki.hypr.land/Configuring/Dwindle-Layout/ for more
dwindle {
    pseudotile = true # Master switch for pseudotiling. Enabling is bound to mainMod + P in the keybinds section below
    preserve_split = true # You probably want this
}

# See https://wiki.hypr.land/Configuring/Master-Layout/ for more
master {
    new_status = master
}

# https://wiki.hypr.land/Configuring/Variables/#misc
misc {
    force_default_wallpaper = -1 # Set to 0 or 1 to disable the anime mascot wallpapers
    disable_hyprland_logo = true # If true disables the random hyprland logo / anime girl background. :(
    enable_swallow = true
}

scrolling {
    fullscreen_on_one_column = true
    column_width = 0.5
    direction = right
    focus_fit_method = 1
}


#############
### INPUT ###
#############

# https://wiki.hypr.land/Configuring/Variables/#input
input {
    kb_layout = us
    kb_variant =
    kb_model =
    kb_options =
    kb_rules =
    accel_profile = flat
    follow_mouse = 1

    sensitivity = 1.0 # -1.0 - 1.0, 0 means no modification.

    touchpad {
        natural_scroll = true
    }
}

# See https://wiki.hypr.land/Configuring/Gestures
gesture = 3, horizontal, workspace

# Example per-device config
# See https://wiki.hypr.land/Configuring/Keywords/#per-device-input-configs for more
device {
    name = epic-mouse-v1
    sensitivity = -0.5
}


###################
### KEYBINDINGS ###
###################

# See https://wiki.hypr.land/Configuring/Keywords/
$mainMod = SUPER # Sets "Windows" key as main modifier
bind = $mainMod,F2,exec,pkill waybar || true && waybar
bind = $mainMod, F1, exec, pkill waybar
bind = $mainMod ALT,L,exec, ~/.config/hypr/scripts/smart_lock.sh

# Example binds, see https://wiki.hypr.land/Configuring/Binds/ for more
bind = $mainMod, T, exec, $terminal
bind = $mainMod, Q, killactive,
#bind = $mainMod, M, exec, command -v hyprshutdown >/dev/null 2>&1 && hyprshutdown || hyprctl dispatch exit
bind = $mainMod, E, exec, $fileManager
bind = $mainMod, V, togglefloating,
bind = $mainMod, R, exec, $menu
bind = $mainMod, P, pseudo, # dwindle
bind = $mainMod, J, layoutmsg, togglesplit # dwindle
bind = $mainMod, F, fullscreen
bind = $mainMod, A,exec,gradia --screenshot
bind = $mainMod, F1, exec,pkill waybar
bind = $mainMod ALT,V, exec, copyq toggle
bind = $mainMod, Y, exec, kitty --class floatterm
binde = CTRL, F1, exec, brightnessctl set 5%-
binde = CTRL, F2, exec, brightnessctl set +5%+
binde = CTRL, F3, exec, wpctl set-volume -l 1.5 @DEFAULT_AUDIO_SINK@ 5%-
binde = CTRL, F4, exec, wpctl set-volume -l 1.5 @DEFAULT_AUDIO_SINK@ 5%+

# Move focus with mainMod + arrow keys
bind = $mainMod, left, movefocus, l
bind = $mainMod, right, movefocus, r
bind = $mainMod, up, movefocus, u
bind = $mainMod, down, movefocus, d

# Switch workspaces with mainMod + [0-9]
bind = $mainMod, 1, workspace, 1
bind = $mainMod, 2, workspace, 2
bind = $mainMod, 3, workspace, 3
bind = $mainMod, 4, workspace, 4
bind = $mainMod, 5, workspace, 5
bind = $mainMod, 6, workspace, 6
bind = $mainMod, 7, workspace, 7
bind = $mainMod, 8, workspace, 8
bind = $mainMod, 9, workspace, 9
bind = $mainMod, 0, workspace, 10

# Move active window to a workspace with mainMod + SHIFT + [0-9]
bind = $mainMod SHIFT, 1, movetoworkspace, 1
bind = $mainMod SHIFT, 2, movetoworkspace, 2
bind = $mainMod SHIFT, 3, movetoworkspace, 3
bind = $mainMod SHIFT, 4, movetoworkspace, 4
bind = $mainMod SHIFT, 5, movetoworkspace, 5
bind = $mainMod SHIFT, 6, movetoworkspace, 6
bind = $mainMod SHIFT, 7, movetoworkspace, 7
bind = $mainMod SHIFT, 8, movetoworkspace, 8
bind = $mainMod SHIFT, 9, movetoworkspace, 9
bind = $mainMod SHIFT, 0, movetoworkspace, 10


# Example special workspace (scratchpad)
#bind = $mainMod, S, togglespecialworkspace, magic
#bind = $mainMod SHIFT, S, movetoworkspace, special:magic
bind = $mainMod SHIFT, S, exec, ~/.config/hypr/scripts/screenshot.sh
# Scroll through existing workspaces with mainMod + scroll
bind = $mainMod, mouse_down, workspace, e+1
bind = $mainMod, mouse_up, workspace, e-1

bind = $mainMod, W, layoutmsg, colresize +conf # 调整当前列宽（按配置步长）
bind = $mainMod, O, layoutmsg, fit all # 让所有窗口自动重新排布，填满空间
bind = $mainMod ALT, O, layoutmsg, fit active # 当前窗口适配空间
bind = $mainMod ALT, P, layoutmsg, promote # 当前窗口变为主窗口
bind = $mainMod, bracketleft, layoutmsg, colresize -0.1 # [ 改变窗口横向占比
bind = $mainMod, bracketright, layoutmsg, colresize +0.1 # ] 改变窗口横向占比
bind = $mainMod, comma, layoutmsg, swapcol l # , → 往左移动列
bind = $mainMod, period, layoutmsg, swapcol r # . → 往右移动列


# Move/resize windows with mainMod + LMB/RMB and dragging
bindm = $mainMod, mouse:272, movewindow
bindm = $mainMod, mouse:273, resizewindow

# Laptop multimedia keys for volume and LCD brightness
bindel = ,XF86AudioRaiseVolume, exec, wpctl set-volume -l 1 @DEFAULT_AUDIO_SINK@ 5%+
bindel = ,XF86AudioLowerVolume, exec, wpctl set-volume @DEFAULT_AUDIO_SINK@ 5%-
bindel = ,XF86AudioMute, exec, wpctl set-mute @DEFAULT_AUDIO_SINK@ toggle
bindel = ,XF86AudioMicMute, exec, wpctl set-mute @DEFAULT_AUDIO_SOURCE@ toggle
bindel = ,XF86MonBrightnessUp, exec, brightnessctl -e4 -n2 set 5%+
bindel = ,XF86MonBrightnessDown, exec, brightnessctl -e4 -n2 set 5%-

# Requires playerctl
bindl = , XF86AudioNext, exec, playerctl next
bindl = , XF86AudioPause, exec, playerctl play-pause
bindl = , XF86AudioPlay, exec, playerctl play-pause
bindl = , XF86AudioPrev, exec, playerctl previous

#切换布局模式（dwindle<->Scrolling）
bind = $mainMod, Z, exec, ~/.config/hypr/scripts/toggle_layout.sh



##############################
### WINDOWS AND WORKSPACES ###
##############################

# See https://wiki.hypr.land/Configuring/Window-Rules/ for more
# See https://wiki.hypr.land/Configuring/Workspace-Rules/ for workspace rules

# Example windowrules that are useful

windowrule {
    # Ignore maximize requests from all apps. You'll probably like this.
    name = suppress-maximize-events
    match:class = .*

    suppress_event = maximize
}

windowrule {
    # Fix some dragging issues with XWayland
    name = fix-xwayland-drags
    match:class = ^$
    match:title = ^$
    match:xwayland = true
    match:float = true
    match:fullscreen = false
    match:pin = false

    no_focus = true
}

# Hyprland-run windowrule
windowrule {
    name = move-hyprland-run

    match:class = hyprland-run

    move = 20 monitor_h-120
    float = yes
}
windowrule {
	name = CopyQ
	match:class = com.github.hluk.copyq
	float = true
}

windowrule {
	name = Gradia
	match:title = Gradia
	float = true
}

windowrule {
	name = FlClash
	match:title = FlClash
	float = true
}

windowrule {
	name = bilibili
	match:class = bilibili
	float = true
	center = true
}
## QQ-main-not-float
windowrule {
  name = qq-main-not-float
  match:class = .*[Qq][Qq].*
  match:title = ^QQ$
  float = false
}
## QQ-child-float
windowrule {
  name = qq-child-float
  match:class = .*[Qq][Qq].*
  float = true
}

windowrule {
  name = thunar-float
  match:class = ^thunar$
  float = true
}

windowrule {
  name = steam-main-not-float
  match:class = ^steam$
  match:title = ^Steam$
  float = false
}

windowrule {
  name = steam-child-float
  match:class = ^steam$
  float = true
}

windowrule {
	name = wechat-main-float
	match:class = ^wechat$
	match:title = ^weixin$
	float = false
}

windowrule {
	name = wechat-child-float
	match:class = ^wechat$
	float = true
}

windowrule {
  name = pavucontrol-popup
  match:class = ^org.pulseaudio.pavucontrol$
  float = true
  center = true
  size = 900 500
}

windowrule {
  name = Bluetooth-popup
  match:class = ^blueman-manager$
  float = true
  center = true
  size = 900 500
}

windowrule {
  name = Waypaper
  match:class = ^waypaper$
  float = true
  center = true
}

windowrule {
  name = alger-child

  match:title = ^(AlgerMusicPlayer ｜ algerkong)$

  float = true
}

windowrule {
  name = floatterm
  match:class = ^floatterm$
  float = true
}

windowrule {
    name = anki
    match:class = anki
    float = true
}
